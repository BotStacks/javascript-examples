export interface IProps {
    count?: number;
    max?: number;
    images?: string[];
}
export declare const GroupCount: ({ count, max, images }: IProps) => any;
export declare const ChatGroupCount: any;
