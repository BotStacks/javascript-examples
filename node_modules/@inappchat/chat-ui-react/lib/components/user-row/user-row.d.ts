import { IUser } from '@inappchat/chat-sdk';
export interface UserRowProps {
    user: IUser;
    isActive?: boolean;
    isSelectable?: boolean;
    isSelected?: boolean;
    className?: string;
    link?: boolean;
    onClick?: () => void;
}
export declare function UserRow({ user, isActive, isSelectable, isSelected, className, link, onClick, }: UserRowProps): any;
export default UserRow;
