export interface SegmentProps {
    items: {
        value: string;
        label: string;
    }[];
    onSelect?: (value: string) => void;
    initialSelection?: string;
    elementWidth?: string;
}
export declare function Segment({ items, initialSelection, onSelect, elementWidth, }: SegmentProps): any;
export default Segment;
