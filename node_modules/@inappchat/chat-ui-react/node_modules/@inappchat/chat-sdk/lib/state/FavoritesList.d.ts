import { IListModel } from './ListState';
import { IMessageType } from './Messages';
export declare const FavoritesList: IListModel<IMessageType, object, {
    setFavorite(id: string, favorite: boolean): void;
}>;
export type IFavoritesListType = typeof FavoritesList;
