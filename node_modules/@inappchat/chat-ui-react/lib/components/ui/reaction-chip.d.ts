export interface ReactionChipProps {
    reaction?: string;
    selected?: boolean | undefined;
    count?: number | undefined;
    onClick?: () => void;
}
declare const ReactionChip: ({ reaction, selected, count, onClick, }: ReactionChipProps) => any;
export default ReactionChip;
