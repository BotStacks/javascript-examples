import { ISimpleType } from 'mobx-state-tree';
import { FMessageFragment } from '../api/ops';
import { IListModel } from './ListState';
import { IMessage, IMessageType } from './Messages';
export declare const MessageList: IListModel<IMessageType, {
    chat: ISimpleType<string>;
}, {
    add(messages: FMessageFragment | FMessageFragment[]): IMessage[];
    readonly sortedAsc: IMessage[];
}>;
