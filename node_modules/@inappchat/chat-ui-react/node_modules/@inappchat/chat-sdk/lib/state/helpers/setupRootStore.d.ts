import type { InAppChatSnapshot, InAppChatStore } from '../Store';
export declare function setupRootStore(rootStore: InAppChatStore): Promise<{
    rootStore: InAppChatStore;
    restoredState: InAppChatSnapshot;
    unsubscribe: () => void;
}>;
