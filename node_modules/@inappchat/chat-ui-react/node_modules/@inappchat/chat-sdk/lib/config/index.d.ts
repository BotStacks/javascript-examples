export type Environment = 'local' | 'dev' | 'prod';
export declare const getEnv: (env?: "prod" | "dev" | "local") => {
    CHAT: string;
    CHATSUB: string;
} | {
    PROVISIONING: string;
    CHAT: string;
    CHATSUB: string;
} | {
    PROVISIONING: string;
    CHAT: string;
    CHATSUB: string;
};
